import React from 'react';

const Admin = () => {
    return (
        <div className="admin-layout">
            <h2>Admin Dashboard</h2>
            {/* Nội dung cho trang Admin */}
        </div>
    );
};

export default Admin; // Xuất mặc định
