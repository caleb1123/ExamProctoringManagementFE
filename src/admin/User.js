import React from 'react';
import './Admin.css'; // Use the same CSS or a different one if necessary

const Users = () => {
    return (
        <div>
            <h2>Users Management</h2>
            {/* You can add user management logic here, such as user list, adding/removing users, etc. */}
            <p>This is the user management page.</p>
            {/* You can add tables, forms, or buttons to manage users */}
        </div>
    );
};

export default Users;
